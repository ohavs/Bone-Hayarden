# תיעוד פרויקט: בונה הירדן המערבי (Bone-Hayarden)

ברוכים הבאים למסמך התיעוד של פרויקט **בונה הירדן המערבי**. מסמך זה מספק סקירה מקיפה של ארכיטקטורת האתר, הטכנולוגיות בשימוש, מבנה הקוד, והשירותים המשולבים.

---

## 1. טכנולוגיות ליבה (Tech Stack)

האתר נבנה תוך דגש על ביצועים גבוהים, קוד נקי, עיצוב יוקרתי (Premium) ונגישות מלאה:
* **Frontend:** HTML5 סמנטי, Vanilla CSS3 (משתני עיצוב מותאמים, גריד, פלקסבוקס ואנימציות), ו-JavaScript (ES6+) אינטראקטיבי.
* **Database & Hosting:** Firebase (Firestore Database, Firebase Storage, ו-Firebase Hosting).
* **ספריות חיצוניות:** [AOS (Animate On Scroll)](https://michalsnik.github.io/aos/) ליצירת הנפשות כניסה מרהיבות וחלקות במהלך הגלילה.
* **פונטים:** שילוב פונטים לוקאליים יוקרתיים (כמו *Outfit* ו-*Inter* או פונטים עבריים שהועלו לתיקיית `fonts/`) הניתנים לשינוי דינמי ישירות מפאנל הניהול.
* **תאימות נגישות (Accessibility):** עמידה בתקן הנגישות הישראלי ת"י 5568 וברמת WCAG 2.1 AA באמצעות פאנל נגישות אינטראקטיבי, ניווט מקלדת מלא, ותיוג ARIA.

---

## 2. מבנה הקבצים בפרויקט (Project Directory Structure)

```text
Bone-2026/
├── index.html            # דף הבית הראשי של החברה (כולל גלריה, אודות ושירותים)
├── projects.html         # עמוד ריכוז כל הפרויקטים (נטענים דינמית מ-Firestore)
├── project.html          # עמוד פרטי פרויקט ספציפי (מציג גלריה, מפרט ופרטים)
├── admin.html            # פאנל ניהול עצמאי, מאובטח ומעוצב (CRUD פרויקטים, צפייה בלידים)
├── accessibility.html    # הצהרת נגישות רשמית ומותאמת לתקן
├── terms.html            # דף תנאי שימוש
├── privacy.html          # דף מדיניות פרטיות
├── main.js               # לוגיקת צד לקוח (ניווט, גלילה חלקה, טפסים, פאזלים, חיבור Firebase)
├── styles.css            # גיליון העיצוב הראשי (קבועי צבעים, גריד, הנפשות ורספונסיביות)
├── fonts.css             # הגדרת פונטים מקומיים
├── firebase-config.js    # הגדרות חיבור ואתחול של Firebase SDK
├── firestore.rules       # חוקי אבטחה של Firestore לשמירה על שלמות הנתונים
├── firebase.json         # הגדרות פריסה והפצה של Firebase CLI
└── .firebaserc           # קישור הפרויקט ל-Firebase Project Alias
```

---

## 3. אינטגרציה עם Firebase

האתר מסתמך על ארכיטקטורת Serverless מלאה המופעלת על ידי Firebase:

### א. Firestore Database
בסיס הנתונים מורכב מהאוספים (Collections) הבאים:
1. **`projects`**:
   * מסמכים המייצגים את פרויקטי החברה.
   * שדות: מזהה, כותרת, מיקום, סוג מגורים, סטטוס (בשיווק, בבנייה, מאוכלס, בתכנון), תמונה ראשית, גלריית תמונות, תיאור מורחב, ומפרט טכני.
2. **`leads`**:
   * שמירת פניות לקוחות מהטפסים השונים.
   * שדות: שם מלא, טלפון, הודעה/סוג פנייה, תאריך ושעה (`timestamp`), ומקור הפנייה (`general` או `noah`).
3. **`settings`**:
   * הגדרות מערכת גלובליות.
   * מסמך `admin` מכיל את השדה `selectedFont` לשליטה דינמית על הפונט המוצג בכל האתר.

### ב. Firebase Storage
שמירת תמונות פרויקטים. פאנל הניהול כולל מנגנון דחיסה וכיווץ אוטומטי של תמונות לפורמט **WebP** בצד הלקוח לפני ההעלאה, כדי להבטיח זמני טעינה מהירים במיוחד.

### ג. Firebase Hosting
הפצה מהירה ומאובטחת בכתובת המותאמת: [bone-h.web.app](https://bone-h.web.app).

---

## 4. תכונות ורכיבים מרכזיים

* **גלריית Lightbox:** מובנית ב-Vanilla JS ללא ספריות כבדות, המאפשרת פתיחה, דפדוף וסגירה של תמונות פרויקט בצורה חלקה.
* **מגירות שירותים אינטראקטיביות:** תפריט שירותים מעוצב ("אקורדיון") המאפשר הרחבה של כל שירות בלחיצה, עם הנפשות כניסה ושימוש במעוינים יוקרתיים כסימוני נקודות.
* **קישוריות חכמה בתיבת נוח:** שלב 2 בתהליך ההזמנה של תיבת נוח מפנה באופן חלק ואלגנטי לטופס יצירת הקשר בתחתית הדף עם הדגשה חזותית המזמינה לחיצה.
* **פאנל הניהול (admin.html):** מערכת ניהול מקיפה בעלת מראה כהה ויוקרתי (Premium Dark UI), המוגנת בסיסמה מוצפנת SHA-256 מול Firestore, המאפשרת יצירה, עריכה ומחיקה של פרויקטים, צפייה וניהול פניות לקוחות (leads), ושינוי גופן האתר.
* **פאנל נגישות מבוסס State שמור:** פאנל הנגישות שומר את העדפות המשתמש (גודל גופן, ניגודיות גבוהה, גווני אפור, הדגשת קישורים, גופן קריא ועצירת אנימציות) בתוך ה-`localStorage` של הדפדפן. ההעדפות נטענות ומיושמות אוטומטית בכל רענון או מעבר בין דפי האתר, ושומרות על עקביות חווית המשתמש.
